# criar kayword

def greet_user(name):
    """Keyword que retorna uma mensagem de saudação."""
    return f"Olá, {name}!"
    

def log_message(message):
    """Keyword que apenas imprime uma mensagem no log."""
    print(f"Mensagem: {message}")